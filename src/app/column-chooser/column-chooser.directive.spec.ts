import { ColumnChooserDirective } from './column-chooser.directive';

describe('ColumnChooserDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnChooserDirective();
    expect(directive).toBeTruthy();
  });
});
